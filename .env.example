# =============================================================================
# Use the SAME Supabase project for local AND production so progress is global
# (same Google account = same user_id = same data on localhost and deployed).
# =============================================================================

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-only (API routes, server actions). Never expose in client.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# App URL for OAuth redirect. Set per environment:
# - Local:  http://localhost:3000
# - Vercel: https://your-app.vercel.app
# Add BOTH URLs in Supabase Dashboard → Auth → URL Configuration → Redirect URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
